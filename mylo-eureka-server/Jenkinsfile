pipeline {
    agent any
    stages {
        stage('Print PWD') {
            steps {
                script {
                    def currentDir = pwd()
                    echo "Current working directory is: ${currentDir}"
                }
            }
        }
        stage('Change to the sub directory') {
            steps {
                sh 'cd mylo-eureka-server'
            }
        }
        stage('Build') {
            steps {
                script {
                    def currentDir = pwd()
                    echo "Current working directory is: ${currentDir}"
                }
                sh 'mvn clean package'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Build Docker Image') {
            steps {
                echo 'Building Docker Image'
            }
        }
    }
}